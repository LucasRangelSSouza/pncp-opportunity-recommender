[project]
name = "pncp-opportunity-recommender"
version = "0.1.0"
requires-python = ">=3.10"

[project.scripts]
pncp-recommender = "pncp_recommender.__main__:main"

[tool.setuptools.packages.find]
include = ["pncp_recommender*"]
