# Project memory

The fixture-first transparent ranking path validates organization-only profiles, filters open opportunities by deadline, scores lexical item overlap plus declared constraints, and emits an explanation with a source link. Three local tests pass. Do not claim real recommendation quality, eligibility assessment, or PNCP historical-data coverage until a pinned public release and benchmark exist.
